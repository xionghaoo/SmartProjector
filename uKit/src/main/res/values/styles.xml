<resources>

    <style name="windowAnimationStyle" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/anim_activity_in</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_activity_out</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_activity_in</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_activity_out</item>
    </style>
    <style name="notAnimation">
    <item name="android:activityOpenEnterAnimation">@null</item>
    <item name="android:activityOpenExitAnimation">@null</item>
    <item name="android:activityCloseEnterAnimation">@null</item>
    <item name="android:activityCloseExitAnimation">@null</item>
    <item name="android:taskOpenEnterAnimation">@null</item>
    <item name="android:taskOpenExitAnimation">@null</item>
    <item name="android:taskCloseEnterAnimation">@null</item>
    <item name="android:taskCloseExitAnimation">@null</item>
    <item name="android:taskToFrontEnterAnimation">@null</item>
    <item name="android:taskToFrontExitAnimation">@null</item>
    <item name="android:taskToBackEnterAnimation">@null</item>
    <item name="android:taskToBackExitAnimation">@null</item>
    </style>
    <style name="HomeTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Fullscreen style -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 设置按钮默认样式，不需要每个布局的地方都设一遍 -->
        <item name="android:buttonStyle">@style/default_button_style</item>
    </style>
    <style name="FullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Fullscreen style -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/notAnimation</item>
        <!-- 设置按钮默认样式，不需要每个布局的地方都设一遍 -->
        <item name="android:buttonStyle">@style/default_button_style</item>
    </style>

    <style name="LauncherScreenTheme" parent="FullScreenTheme">
        <item name="android:windowBackground">@drawable/splash_preview</item>
    </style>

    <style name="TranslucentFullScreenTheme" parent="FullScreenTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <!-- 设置按钮默认样式，不需要每个布局的地方都设一遍 -->
        <item name="android:buttonStyle">@style/default_button_style</item>
    </style>

    <style name="menu_item_text" >
        <item name="android:textSize">@dimen/ubt_font_40px</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#555B67</item>
        <item name="android:layout_centerVertical">true</item>
    </style>

    <!-- 默认按钮样式：文字不大写 -->
    <style name="default_button_style" parent="Widget.AppCompat.Button" >
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
